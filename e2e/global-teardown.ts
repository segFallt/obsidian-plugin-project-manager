// Global teardown — no-op for now.
// Per-suite cleanup (temp vault deletion, Obsidian close) is handled in afterAll().
export default async function globalTeardown() {
  // Nothing to do globally
}
